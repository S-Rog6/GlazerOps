create table if not exists public.sites (
  id bigint generated by default as identity primary key,
  site_name text not null,
  address_1 text null,
  address_2 text null
);

create unique index if not exists ux_sites_name_addr
on public.sites (site_name, coalesce(address_1,''), coalesce(address_2,''));