@page "/auth/callback"
@inject Supabase.Client Sb
@inject NavigationManager Nav

<h3>Signing you in…</h3>

@code {
    protected override async Task OnInitializedAsync()
    {
        // Pull session from persistence / refresh if needed :contentReference[oaicite:3]{index=3}
        await Sb.Auth.RetrieveSessionAsync();

        // If you want to verify:
        // var session = Sb.Auth.CurrentSession; :contentReference[oaicite:4]{index=4}

        Nav.NavigateTo("/");
    }
}